#ifndef READ_DATA_MAP_H
#define READ_DATA_MAP_H

#include <string>
#include <vector>
#include <map>

void read_data_map (const std::string &map_file, std::map<std::string, std::vector<std::string>> &data_map);

#endif // READ_DATA_MAP_H
